# `ConfigNode`

A single line of configuration and its place in the hierarchy. Nodes expose the
same query API as [`Config`](config.md), plus navigation over their own position
in the tree (`path`, `ancestors`, `descendants`, `root`).

::: networkconfparse.ConfigNode
